/**
 * Convenience re-export — use `createClient` from here for browser-side code.
 *
 * For server components / route handlers use `@/lib/supabase/server` instead.
 */
export { createClient } from '@/lib/supabase/client';
